name: Blue-Green CI

on:
  workflow_dispatch: {}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build
        run: echo "build" # TODO_CHECKLIST - [ ] Add build commands

  deploy-green:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to green
        run: echo "deploy green" # TODO_CHECKLIST - [ ] Implement green deploy

  deploy-blue:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to blue
        run: echo "deploy blue" # TODO_CHECKLIST - [ ] Implement blue deploy

  switch-traffic:
    runs-on: ubuntu-latest
    needs: [deploy-green, deploy-blue]
    steps:
      - name: Manual switch placeholder
        run: bash cicd/switch_traffic.sh
